# Update to latest macOS
# setup gitub ssh keys
# install git

# clone github dotfiles repo
# copy dotfiles into home directory

# install xcode
# install homebrew
# install brew packages, casks and mas apps
